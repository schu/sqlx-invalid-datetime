[package]
name = "sqlx-invalid-datetime"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
sqlx = { version = "0.8", features = ["sqlite", "runtime-tokio", "tls-rustls", "chrono"] }
tokio = { version = "1.0", features = ["full"] }
